@{
    ViewBag.Title = "Home Page";
}

@using (Html.BeginForm("Command", "Home"))
{
    <div class="form-group">
        <div class="col-md-10">
            @Html.Editor("poruka", new { htmlAttributes = new { @class = "form-control" } })
        </div>
  </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Simulacij" class="btn btn-default" />
            </div>
        </div>
        }
